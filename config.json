{
  "_comment": [
    "Runtime policy. The client asked for a parameter to control how strict the",
    "assistant is, and accepted a config file rather than a dashboard for it.",
    "",
    "strictness picks a preset:",
    "  strict    - highest refusal rate, lowest hallucination risk. Every",
    "              knowledge answer must be strongly supported, and anything",
    "              marginal goes to a human.",
    "  balanced  - the default.",
    "  relaxed   - answers more questions, including weakly-supported ones.",
    "              More natural to talk to, and more likely to be wrong.",
    "",
    "Overrides below take precedence over the preset when set to a number."
  ],
  "strictness": "balanced",

  "_router_mode": [
    "Which classifier decides the route:",
    "  nlu     - lexical only. The default, and the fallback whenever a",
    "            provider is unavailable or an LLM call fails.",
    "  shadow  - both run, the LEXICAL result is used, both are recorded.",
    "            Use this to gather evidence before switching.",
    "  llm     - the model decides, lexical classifier as fallback.",
    "",
    "The compliance guardrail, the identity check and the card state machine",
    "stay in code in every mode. This switch only chooses who proposes the",
    "intent, never who is allowed to act on it."
  ],
  "router_mode": "nlu",

  "overrides": {
    "min_grounding": 0.04,
    "min_relevance": null,
    "high_confidence": null
  },

  "presets": {
    "strict":   { "min_grounding": 0.70, "min_relevance": 0.44, "high_confidence": 0.50 },
    "balanced": { "min_grounding": 0.55, "min_relevance": 0.36, "high_confidence": 0.55 },
    "relaxed":  { "min_grounding": 0.40, "min_relevance": 0.24, "high_confidence": 0.62 }
  }
}
